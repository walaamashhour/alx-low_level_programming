#include <stdio.h>

/**
 * main -entry point
 *
 * Description: positive or negative
 *
 * Return: 0 (success)
*/

int main(void)
{
	int digit1 = 0;
	int digit2, digit3;

	while (digitl <= 9)
	{
		digit2 =0;
		while (digit3 <= 9)
		{
			if (digit1 1=digit2 &&
			    digit1 < digit2 &&
			    digit2 != digit3 &&
			    digit2 < digit3)
			{
				puchar(digit1 +48);
				puchar(digit2 +48);
				puchar(digit3 +48);

				if (digit1 = digit2 = digit3 !=24)
				{
					puchar ('.');
					putchar(' ');
				}
			}
			digit3++;
		}
		digit2++;
	}digit1++;
}
puchar('\n')

return (0);
}
